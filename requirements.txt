# TFM — MABe Mouse Behavior Detection
# Pinned to the versions verified in the project .venv (Python 3.14, Windows).
# Install:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
#
# Note: torch is pinned to the CPU build actually used here. For a CUDA build,
# install the matching wheel from https://pytorch.org first, then the rest.

# --- core scientific stack ---
numpy==2.4.4
scipy==1.17.1
pandas==3.0.2
scikit-learn==1.8.0
statsmodels==0.14.6

# --- deep learning ---
torch==2.11.0

# --- config / data IO ---
omegaconf==2.3.0
hydra-core==1.3.2
pyarrow==24.0.0
h5py==3.16.0

# --- viz / embeddings / utils (notebooks + diagnostics) ---
matplotlib==3.10.9
seaborn==0.13.2
umap-learn==0.5.12
joblib==1.5.3
tqdm==4.67.3

# --- hyperparameter search ---
# Imported unguarded by notebook 02a, which tunes the correction weights.
optuna==4.8.0

# --- tests ---
pytest==8.3.4

# ---------------------------------------------------------------------------
# Optional. Every one of these is imported behind a try/except, so the pipeline
# runs without them; each unlocks one specific piece.
#
#   torch-geometric   GATv2 message passing in src/models/graph.py. Without it
#                     the model falls back to a dense attention implementation
#                     that gives the same results on a 7-node graph.
#   lightgbm          the LGBMBaseline arm of src/models/baseline.py.
#   shap              SHAP feature importances in src/data/feature_pipeline.py.
#   dcor              distance correlation in the redundancy analysis; slow,
#                     and off by default (features.redundancy.compute_dcor).
#   tabulate          markdown tables in scripts/make_lb_summary.py; without it
#                     the summary falls back to plain text.
# ---------------------------------------------------------------------------
